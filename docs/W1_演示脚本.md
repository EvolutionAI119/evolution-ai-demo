# W1 演示脚本 · 5 分钟主人走查路线

**日期**：2026-06-21
**版本**：v1.0
**目的**：让主人在 5 分钟内看到 W1 周级完工的核心能力

---

## 0. 走查前置

```bash
# 4 服务全启（如果未启）
cd /app/data/所有对话/主对话/EVOLUTION_AI_DEMO
redis-server --daemonize yes --port 6379
pip install -q -r backend/requirements.txt celery redis trimesh numpy scipy loguru websockets pydantic-settings
(setsid nohup env PYTHONPATH=. python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 </dev/null >logs/uvicorn_boot.log 2>&1 &)
(setsid nohup env PYTHONPATH=. celery -A backend.tasks.celery_app worker -l info --concurrency=2 --logfile=logs/celery_worker.log </dev/null >logs/celery_worker_boot.log 2>&1 &)
cd frontend && (setsid nohup env node /app/data/所有对话/主对话/EVOLUTION_AI_DEMO/frontend/node_modules/vite/bin/vite.js /app/data/所有对话/主对话/EVOLUTION_AI_DEMO/frontend/ </dev/null >/tmp/vite_dev.log 2>&1 &)

# 验证
curl -s http://127.0.0.1:8000/health   # → {"status":"ok"}
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5173/   # → 200
```

打开浏览器：**http://127.0.0.1:5173/**

---

## 1. Home 首页（30s）

**目的**：看整体平台 + 6 入口卡片 + 4 服务状态

- 浏览器打开 `http://127.0.0.1:5173/`
- 看到 **EVOLUTION AI 标题** + 6 张入口卡片（造型设计 / 质量评估 / AI 优化 / 分镜脚本 / 方案管理 / 演示视频）
- 页面底部有 **服务状态条**：uvicorn / Celery / Redis / Vite 4 灯全绿
- 顶部右上角 **中/英 切换按钮**——点一下切到 English，所有文案变英文

**验收点**：
- ✅ 6 入口卡片点击能跳转
- ✅ 4 服务状态全绿
- ✅ 中英切换实时生效（无刷新）

---

## 2. Designer 造型设计（45s）

**目的**：看 11 参数滑块 + 预设形状 + 实时联动

- 点首页 **"造型设计"** 卡片（或导航 Designer）
- 看到 **4 个预设按钮**：car_body / sphere / simple_curve / wing
- 点 **car_body** → 加载 11 维默认参数
- **拖动任意滑块**（如 车身长度 L）→ 右侧预览实时联动
- 看顶部进度条 / 状态栏

**验收点**：
- ✅ 4 预设可切换
- ✅ 11 滑块拖动流畅
- ✅ 数值与预览实时联动

---

## 3. AI 优化 + WS 实时（90s，最核心）

**目的**：看 WS 实时推送 + 收敛曲线

- 切到 **AI 优化** 页（导航 /optimize）
- 选 **car_body** + max_iter=12
- 点 **启动优化** → 后端异步任务开始
- **立即跳到 OptimizeDetail**（WS 自动连上）
- 看 **实时进度条** + 11 点收敛曲线实时更新
- **顶部状态指示**：`● 实时`（绿色）
- 1.8s 后任务完成 → 自动显示 **SUCCESS** + 收敛曲线 + g2 改善

**验收点**：
- ✅ 进度条实时更新（不卡顿、不跳）
- ✅ 11 点曲线连续绘制
- ✅ 状态指示从 connecting → open → closed
- ✅ SUCCESS 时含 13 点 convergence_curve

**故障演示**（可选）：
- 在 Network 面板断网 → 状态变 `↻ 重连中`（黄色）
- 1s/2s/4s 指数退避重连
- 网络恢复 → 状态回 `● 实时`

---

## 4. Quality 质量评估（45s）

**目的**：看 4 预设 + G0/G1/G2 连续性卡片

- 导航 /quality
- 看到 **4 个预设** 评估卡片（sphere / car_body / simple_curve / wing）
- 点 **car_body** → 显示 g2 数量、g2_ratio、曲面等级 D
- 看 G0/G1/G2 三级连续性说明

**验收点**：
- ✅ 4 预设可对比
- ✅ g2 数字 + 等级可视化
- ✅ G0/G1/G2 解释清晰

---

## 5. Projects 方案管理（45s）

**目的**：看列表 + 筛选 + 分页

- 导航 /projects
- 看到 **表格**（el-table）展示已保存方案
- 表头有筛选 / 排序
- 底部有分页器

**验收点**：
- ✅ 表格列对齐
- ✅ 筛选 / 排序生效
- ✅ 分页可翻

---

## 6. Storyboard 分镜（30s）

**目的**：看 6 镜头卡片

- 导航 /storyboard
- 看到 **6 张镜头卡片**（每个故事板 6 个分镜）
- 卡片显示镜头名 + 描述 + 缩略图

**验收点**：
- ✅ 6 卡片等宽对齐
- ✅ 点击进详情页 /storyboard/:id

---

## 7. 错误兜底（15s）

**目的**：看 404 / 500 页面

- 地址栏输 `http://127.0.0.1:5173/no-such-page` → **404 · 页面走丢了**（江湖风文案）
- 故意停后端：`pkill -9 -f "uvicorn backend.main"` → 刷新 → **500 · 服务挂了** 页面

**验收点**：
- ✅ 404 / 500 页面有主人口吻
- ✅ "回首页" 按钮可点

---

## 8. 全链路 E2E 一句话（10s，主人选看）

```bash
cd /app/data/所有对话/主对话/EVOLUTION_AI_DEMO
python3 logs/w1d5_e2e.py
# → 30/30 通过 (0 失败)
# → 🎉 W1 全链路打通！
```

涵盖：后端 21 REST + health + 4 抽样 + 前端 12 路由 + 11 视图 + 2 错误页 + i18n 154×2 key + 13 模块 + 4 文件 utils + 9 文件 api + 4 文件 stores + WS 4 消息 + 11 PROGRESS + SUCCESS 含曲线 + 5 历史 E2E 脚本。

---

## 时间分配

| 段 | 内容 | 时长 |
|---|---|---|
| 0 | 4 服务启动 + 验证 | 30s |
| 1 | Home 首页 | 30s |
| 2 | Designer 造型 | 45s |
| 3 | AI 优化 + WS | 90s |
| 4 | Quality 质检 | 45s |
| 5 | Projects 方案 | 45s |
| 6 | Storyboard 分镜 | 30s |
| 7 | 错误兜底 | 15s |
| 8 | E2E 一句话 | 10s |
| **合计** | | **5min** |

---

## 关键指标速查

| 指标 | 数值 |
|---|---|
| 后端 REST 端点 | 21 |
| WebSocket 端点 | 1（4 消息类型） |
| 前端路由 | 12 |
| 前端页面 | 11（9 主 + 2 错误） |
| i18n key | 154（13 模块 × zh/en） |
| E2E 验证项 | 30（D5 整合）+ 13（D4 WS）+ 42（D3 页面）+ 19（D2 API）+ 5（D1 骨架）+ 15（D2.5 bug fix）= 124 |
| 文档/报告 | 5（D1/D2/D2.5/D3/D4/D5 完结） |
| 截图演示 | w1d3_overview.png + w1d4_overview.jpg + w1d4_overview.png |

---

**准备好接收主人验收了。**
